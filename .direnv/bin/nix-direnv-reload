#!/usr/bin/env bash
set -e
if [[ ! -d "/home/vabyz971/Documents/GITHUB/CreativeSuiteOpen" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/home/vabyz971/Documents/GITHUB/CreativeSuiteOpen")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/home/vabyz971/Documents/GITHUB/CreativeSuiteOpen" true

# Update the mtime for .envrc.
# This will cause direnv to reload again - but without re-building.
touch "/home/vabyz971/Documents/GITHUB/CreativeSuiteOpen/.envrc"

# Also update the timestamp of whatever profile_rc we have.
# This makes sure that we know we are up to date.
touch -r "/home/vabyz971/Documents/GITHUB/CreativeSuiteOpen/.envrc" "/home/vabyz971/Documents/GITHUB/CreativeSuiteOpen/.direnv"/*.rc
