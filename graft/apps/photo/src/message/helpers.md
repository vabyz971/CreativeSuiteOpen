# apps/photo/src/message/helpers.rs

- Tool · enum · L22-L32 — pub enum Tool
- PanelType · enum · L35-L39 — pub enum PanelType
- OffsetAxis · enum · L42-L45 — pub enum OffsetAxis
- MaskTarget · struct · L49-L52 — pub struct MaskTarget
- DestructiveOp · enum · L56-L60 — pub enum DestructiveOp
- DestructiveResult · struct · L67-L75 — pub struct DestructiveResult
- DecodedLayer · struct · L80-L80 — pub struct DecodedLayer(pub crate::layers::PixelLayer);
- fmt · function · L82-L88 — fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result
- PendingPaint · struct · L95-L100 — pub struct PendingPaint
