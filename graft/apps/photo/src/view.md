# apps/photo/src/view.rs

- view · function · L27-L224 — pub fn view(app: &PhotoApp, window: iced::window::Id) -> Element<'_, Message>
- subscription · function · L232-L241 — pub fn subscription(app: &PhotoApp) -> Subscription<Message>
- keyboard_filter · function · L244-L256 — fn keyboard_filter(
