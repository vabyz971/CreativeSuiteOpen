# apps/photo/src/message.rs

- Tool · enum · L27-L37 — pub enum Tool
- PanelType · enum · L40-L44 — pub enum PanelType
- OffsetAxis · enum · L47-L50 — pub enum OffsetAxis
- DecodedLayer · struct · L54-L54 — pub struct DecodedLayer(pub PixelLayer);
- fmt · function · L56-L62 — fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result
- PendingPaint · struct · L69-L74 — pub struct PendingPaint
- Message · enum · L77-L342 — pub enum Message
- MaskTarget · struct · L346-L349 — pub struct MaskTarget
- DestructiveOp · enum · L353-L357 — pub enum DestructiveOp
- DestructiveResult · struct · L364-L369 — pub struct DestructiveResult
