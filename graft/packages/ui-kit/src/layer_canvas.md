# packages/ui-kit/src/layer_canvas.rs

- DisplayLayer · struct · L56-L68 — pub struct DisplayLayer
- LayerCanvas · struct · L70-L80 — pub struct LayerCanvas<Message>
- screen_to_doc · function · L85-L95 — fn screen_to_doc(&self, p: Point, bounds: Rectangle) -> (f32, f32)
- new · function · L97-L110 — pub fn new(
- with_layers · function · L113-L116 — pub fn with_layers(mut self, layers: Vec<DisplayLayer>) -> Self
- with_view · function · L119-L123 — pub fn with_view(mut self, pan: Vector, zoom: f32) -> Self
- with_tool · function · L126-L129 — pub fn with_tool(mut self, tool: CanvasTool) -> Self
- with_selection · function · L132-L135 — pub fn with_selection(mut self, sel: Option<Rectangle>) -> Self
- view · function · L139-L147 — pub fn view<'a, Message>(canvas: LayerCanvas<Message>) -> Element<'a, Message>
- State · struct · L154-L159 — pub struct State
- State · type · L165-L165 — type State = State;
- Primitive · type · L166-L166 — type Primitive = CompositePrimitive;
- update · function · L168-L338 — fn update(
- draw · function · L340-L355 — fn draw(
- mouse_interaction · function · L357-L381 — fn mouse_interaction(
- config_hash · function · L389-L408 — fn config_hash(layers: &[DisplayLayer], doc: (f32, f32)) -> u64
- CompositePrimitive · struct · L411-L419 — pub struct CompositePrimitive
- Pipeline · type · L422-L422 — type Pipeline = CompositePipeline;
- prepare · function · L424-L433 — fn prepare(
- draw · function · L435-L438 — fn draw(&self, _pipeline: &Self::Pipeline, _render_pass: &mut wgpu::RenderPass<'_>) -> bool
- render · function · L440-L448 — fn render(
- SHADER · constant · L455-L455 — const SHADER: &str = include_str!("shaders/layer_blend.wgsl");
- Params · struct · L459-L470 — struct Params
- CompositePipeline · struct · L476-L492 — pub struct CompositePipeline
- LayerTex · struct · L494-L498 — struct LayerTex
- Accum · struct · L500-L505 — struct Accum
- new · function · L508-L641 — fn new(device: &wgpu::Device, queue: &wgpu::Queue, format: wgpu::TextureFormat) -> Self
- trim · function · L643-L643 — fn trim(&mut self) {}
- scene_bg · function · L649-L681 — fn scene_bg(
- ensure_accum · function · L683-L713 — fn ensure_accum(&mut self, size: (u32, u32))
- prepare · function · L715-L815 — fn prepare(&mut self, prim: &CompositePrimitive, device: &wgpu::Device, queue: &wgpu::Queue)
- ALIGN · constant · L747-L747 — const ALIGN: u32 = 256;
- write_params · function · L817-L820 — fn write_params(&self, params: &Params)
- render · function · L822-L968 — fn render(
